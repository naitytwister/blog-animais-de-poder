{
  "name": "animotem-ratings",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "wrangler dev --local",
    "deploy": "wrangler deploy",
    "db:create": "wrangler d1 create animotem-ratings",
    "db:local": "wrangler d1 execute animotem-ratings --local --file=./schema.sql",
    "db:remote": "wrangler d1 execute animotem-ratings --remote --file=./schema.sql",
    "test": "node test/worker.test.mjs"
  },
  "devDependencies": {
    "wrangler": "^4.120.0"
  }
}
